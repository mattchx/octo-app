import { db } from '..';
// import { InsertUser, usersTable } from '../schema/links';

export async function createUser() {

  // const response = await db.insert(usersTable).values(data);
  // console.log("RESPONSE!!!!!", response)
}